<% title  "Lista de Produtos" %>

<h1>Lista de Produtos</h1><br />
<p>Verifique abaixo a lista dos Produtos cadastrados no sistema.</p><br />
<br />
<p style="text-align: right;"><%= submit_tag "+ Criar novo",:type=>"button",:onclick=>"window.open('"+new_admin_line_category_product_path(@line, @category)+"','_self');", :class => "Bt_enviar"%> </p>
<br />
<table cellspacing="0" class="tablesorter">
  <thead> 
  <tr>
    <th style="width:30%;">Nome</th>
    <th style="width:35%;">Descrição</th>
    <th colspan="4" style="width:35%;">Ações</th>
  </tr>
  </thead> 
  <tbody>
  <% for product in @products %>
    <tr>
      <td><%= product.name %></td>
      <td><%= product.description %></td>
      <td><%= link_to "Visualizar", admin_line_category_product_path(@line, @category,product) %></td>
      <td><%= link_to "Editar", edit_admin_line_category_product_path(@line, @category,product)%></td>
      <td><%= link_to "Excluir", admin_line_category_product_path(@line, @category,product), :confirm => 'Are you sure?', :method => :delete %></td>
	  <td><%= link_to "Cores", admin_line_category_product_colors_path(@line, @category, product)%></td>
    </tr>
  <% end %>
  </tbody>
</table>
